{{template "base" .}}

{{define "content"}}
  <div class="container">
    <div class="row">
      <div class="col">
        <h1> This is about page.</h1>
        <p> This came from the template: {{index .StringMap "test"}}</p>
        <p>
          {{ if ne (index .StringMap "remote_ip") ""}}
            tour remote ip addess is {{(index .StringMap "remote_ip")}}
            {{else}}
              IDK
            {{end}}
        </p>
      </div>
    </div>
  </div>
{{end}}